env: ras-rm-sandbox
namespace: dev
publicIP: false

replicas: 1
rollingUpdate:
  maxSurge: 1
  maxUnavailable: 1

gcp:
  project: ras-rm-sandbox

image:
  devRepo: eu.gcr.io/ons-rasrmbs-management
  name: banner-api
  tag: latest
  pullPolicy: Always

container:
  port: 8000
service:
  port: 8080